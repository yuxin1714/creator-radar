"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, Check, LoaderCircle, Save } from "lucide-react";
import { Button } from "@/components/ui/button";
type Project={id:string;title:string;idea:string|null;output_language:string;status:string;body:string|null;updated_at:string};
export function CreationDetail({projectId}:{projectId:string}){const [item,setItem]=useState<Project|null>(null),[title,setTitle]=useState(""),[idea,setIdea]=useState(""),[body,setBody]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{fetch(`/api/creation-projects/${encodeURIComponent(projectId)}`,{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.message);setItem(d);setTitle(d.title);setIdea(d.idea||"");setBody(d.body||"")}).catch(e=>setMessage(e.message||"无法读取项目。"))},[projectId]);async function save(){setBusy(true);setMessage("");try{const r=await fetch(`/api/creation-projects/${encodeURIComponent(projectId)}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({title,idea:idea||null,body:body||null})});const d=await r.json();if(!r.ok)throw new Error(d.message);setItem(d);setMessage("草稿已保存。")}catch(e){setMessage(e instanceof Error?e.message:"保存失败。")}finally{setBusy(false)}}if(!item&&!message)return <div className="detail-loading"><LoaderCircle className="spin"/>正在读取创作项目…</div>;if(!item)return <><Link className="back-link" href="/creation"><ArrowLeft size={15}/>返回创作空间</Link><section className="panel detail-error"><h1>无法打开项目</h1><p>{message}</p></section></>;return <><Link className="back-link" href="/creation"><ArrowLeft size={15}/>返回创作空间</Link><section className="panel creation-editor"><div className="section-heading"><div><p className="eyebrow">CREATION DRAFT</p><h1>{item.title}</h1><p>草稿项目 · {item.status}</p></div><span className="neutral-label">{new Date(item.updated_at).toLocaleDateString("zh-CN")}</span></div><label>项目标题<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>创作想法<textarea value={idea} onChange={e=>setIdea(e.target.value)} placeholder="记录主题、受众或想表达的观点"/></label><label>正文草稿<textarea className="body-editor" value={body} onChange={e=>setBody(e.target.value)} placeholder="在这里写下正文；GPT 生成后也会保存到这里"/></label><div className="editor-actions"><Button onClick={save} disabled={busy||!title.trim()}>{busy?<LoaderCircle className="spin" size={15}/>:<Save size={15}/>}保存草稿</Button>{message&&<span><Check size={15}/>{message}</span>}</div><div className="quiet-note">GPT 生成、优化和导出将在配置模型后开放。</div></section></>}
