# 生成历史

创作项目新增生成历史，可查看最近 50 次方向、正文、反馈优化记录。任务中心链接携带 generation ID，自动展开对应记录；超出最近 50 次的指定记录也可单独读取。旧方向可重新用于生成正文，继续沿用当时输入快照。

正文需明确点击“载入此生成稿”才替换编辑器，保存后才写入草稿版本。查看历史不调用模型、不改变已保存正文。接口按项目所有权校验，不返回内部完整提示词或参考材料快照。

验证：43 项后端测试、前端类型检查通过；真实已有项目验证方向深链接、切换正文、原稿保持及明确载入，不修改用户保存数据。
