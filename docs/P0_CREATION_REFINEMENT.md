# 反馈优化

创作项目页新增修改意见输入和“按反馈优化正文”。提交前保存编辑器当前内容，再冻结当前正文、反馈、平台语言、完整 Skill 和参考材料。反馈限制 5000 字符；空正文或空意见不能发起优化。任务运行期间拒绝重复生成。

优化结果先显示预览，用户手动采用后才能替换编辑器内容；保存后写入版本历史。模型被要求仅输出完整修订稿、保留未要求改变的内容、不编造事实或产品能力。反馈文本当前不做独立自动暂存；已提交反馈存于生成输入记录。

验证：31 项后端测试通过，前端类型检查通过，浏览器模拟确认保存当前稿、正确发送反馈、显式采用。

真实公司模型验收：项目 `ba493bf6-9b74-4f8a-a276-3eaf1ae2a81f`，生成 `1333e401-64e3-4042-87d5-5e40363cbdfd`，2667 字符。浏览器确认优化成功且原正文未被覆盖。本轮保留预览，未代用户采用；输出质量仍需人工审核。
