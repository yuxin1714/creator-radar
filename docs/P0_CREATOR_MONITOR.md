# 抖音创作者与每日检查

2026-09-08：用户指定主页 `https://v.douyin.com/DR7mfgFFKhw/`，确认每天检查一次。已验证为“柱子哥TzFilm”，工作台记录 `1de14284-9ed8-4471-8023-0c19077a9405`。

## 已实现

- 对标创作者页面接受抖音主页分享内容或完整主页。短链只经过允许的平台跳转，保存时清除分享追踪参数。通过 TikHub 主页身份核验后保存，重复添加不重复创建。
- 首次读取近期一页作品，本样本实际返回 23 条；后续检查最多三页，已收录作品按平台 ID 去重并更新元数据。作者身份必须与监控主页一致。达到分页上限显示提示，不能视为完整历史同步。
- 每个主页可开启/暂停每日检查、手动检查，显示上次完成、下次计划、新增数量和错误。持久化下次时间，每次检查后约 24 小时再次到期。API 每分钟检查到期项；停机后启动补查，不会把错过的每一天逐次重放。
- 今日情报显示最近 24 小时新收录；情报流可查看单个创作者或全部记录。明确区别收录时间与发布时间，不将未经分析的内容包装成质量推荐。可进入作品详情、逐字稿与分析流程。
- 主页检查状态接入统一任务中心。检查仅获取作品信息，不自动批量调用 ASR 或 GPT。

## 验证与供应商限制

42 项后端测试、前端类型检查、生产构建通过。测试覆盖去重、暂停、下次检查、身份隔离和主页解析。真实浏览器已完成添加主页、每日检查偏好、23 条收录、情报流及详情跳转；390px 移动视口无横向溢出。

首次作品请求 HTTP 400，重试成功收录 23 条；后续复查又返回 400，已有数据没有丢失或重复。由此不能宣称监控接口已稳定验收。官方备用 App v3 接口测试返回 HTTP 402，未将其接入自动回退。

提供给 TikHub 支持的信息（不含 Key）：

- 主接口：`GET /api/v1/douyin/web/fetch_user_post_videos`
- 参数：`sec_user_id=MS4wLjABAAAAjN32ZoC90W_FXxpeck2ATV5PCQcnnHM2cSzm8SHdcGCEC3P_fxGweCSTutk3Mvqq`，`max_cursor=0`，`count=20`
- 失败请求 ID：`fe6d8a1d-1e4a-4d89-9142-59d3d7105c83`
- 返回 HTTP 400，供应商提示请求失败、核对参数并联系支持，且声明该失败请求不扣费。
- 备用接口：`GET /api/v1/douyin/app/v3/fetch_user_post_videos` 返回 HTTP 402；需要用户确认账号套餐/余额/接口权限。

本地每日检查已启用，但电脑、Docker 和 API 必须运行；没有部署云端常驻服务。当前失败仍保留到次日的计划，也可从页面手动再试。
