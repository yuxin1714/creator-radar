# 三个创作方向与选向生成

2026-09-08：创作项目编辑页新增“生成三个创作方向”。每张卡包含标题、立意、开场及提纲，选择一张后生成正文；保留直接生成草稿入口。

方向与正文均先保存配置再提交任务。方向生成会冻结平台、语言、Skill 完整内容和版本、参考材料、创作想法；选向生成沿用这个快照，并使用当前正文作为已有草稿。若需要新配置，应重新生成方向。

后台拒绝项目的重复运行任务、其他项目的方向以及不完整的选择；模型方向必须符合恰好三项的结构。正文仅作为预览，明确采用后才载入编辑器，保存后写入草稿及版本历史。刷新可恢复最新方向，但历史方向选择入口尚未实现。

验证：30 项后端测试通过，含冻结输入、重复任务、方向选择、无效模型响应和原稿保护；前端类型检查、生产构建通过；浏览器模拟验证三张卡、刷新、选择请求和显式采用通过。此项尚未完成真实公司模型调用验收：当前 Docker 数据库服务未就绪，不能将模拟验证视为真实验收。

仍使用本地 BackgroundTasks，进程重启后的任务恢复与统一任务中心另行完善。

## 真实验收补充

Docker 恢复后，公司模型真实链路已通过：项目 `ba493bf6-9b74-4f8a-a276-3eaf1ae2a81f` 生成三方向记录 `a9effe43-54ba-4724-980f-8eab70468299`，选择第一个方向生成正文记录 `6b4f0c68-d1ce-4d2c-8b14-c1772b5b24b7`（2952 字符）。浏览器确认方向和正文均未自动覆盖原稿。此轮保留正文预览，没有代用户采用。

同时修复真实请求暴露的来源校验问题：Next 内部可能把 127.0.0.1 转换为 localhost，因此按实际 Host 校验浏览器 Origin。模拟路由测试不能替代该真实代理验证。
