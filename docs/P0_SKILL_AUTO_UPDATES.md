# 远程 Skill 每日更新

2026-09-09：远程 Skill 可在设置页开启或关闭每日更新检查。仅对已启用的远程源执行；新注册源默认手动模式。按用户此前要求，TKI 已开启每日检查，首次真实检查返回 CURRENT（已是最新版本）。

检查由本地服务运行，每分钟扫描到期计划、约 24 小时检查一次。修改规则失败或网络失败保留当前版本；文档无效进入阻断状态。同步读取文档而不执行仓库代码，已有生成记录及输入快照不被更改。手动与自动同步在本地单 API 进程内互斥，避免并行同步覆盖版本。

验证：63 项后端测试、前端类型检查和生产构建通过；包括到期去重、失败保留旧版本、同步互斥，以及浏览器开关与设置版本校验。真实 TKI 检查成功，没有调用 GPT 或 TikHub。

自动检查依赖本机服务运行；停机后启动检查到期计划，不是云端常驻。页面可随时关闭。
