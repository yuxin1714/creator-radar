# 创作项目完成、归档与恢复

项目支持草稿、已完成、已归档三种状态，创作空间可按状态筛选，默认隐藏归档项目。正文保存后可标记完成；归档保留正文、生成记录和保存历史，可明确恢复为草稿。

归档项目禁止保存改写和模型生成，仍允许阅读与导出。完成稿编辑内容或配置后保存自动回到草稿。生成任务运行中不可切换状态；状态请求必须带原更新时间，防止多窗口覆盖。状态切换前页面要求先保存未提交修改。

验证：56 项后端测试、前端类型检查通过；浏览器模拟完成→归档→恢复、正文保留与归档只读。测试未更改用户真实项目状态。
