# 阅读型草稿预览

当前生成、生成历史和保存版本预览使用 Markdown 阅读视图，保留“查看原文”切换。导出和采用仍使用原始内容，不更改保存格式。

使用 react-markdown，跳过原始 HTML，外部图片只显示文字占位，不自动加载。链接使用默认安全协议处理并在新窗口打开。标题、列表、引用与段落已补阅读样式。

验证：前端类型检查与生产构建通过；浏览器验证标题/列表、原文精确一致、脚本不执行、外部图片不请求，以及危险链接协议被过滤。
