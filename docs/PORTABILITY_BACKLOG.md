# 待讨论：跨设备复制与迁移工作台
记录日期：2026-08-31。用户要求先记录，下一次专门讨论；本轮不创建 Skill、不执行迁移。

目标：更换电脑后，按标准流程恢复同一版本的 Creator Radar，而非让 AI 重新生成代码。
两种模式：全新工作台；恢复原工作台（包含数据库、媒体、创作项目和安全迁移的凭证）。
拟议交付：版本化代码包/私有仓库、安装与验收脚本、备份恢复流程、组织这些流程的部署 Skill。
存储偏好：工具、依赖、缓存、数据优先非 C 盘；路径参数化，不能假设每台设备都有 F 盘。
约束：不在仓库/Skill 内存真实 Key；不打包 node_modules/.venv 当作跨设备运行环境；不覆盖已有数据。
当前仅在一台 Windows 电脑验证；容器跨平台锁定、版本兼容、恢复演练尚未完成。
沙箱权限错误是本机个案，不应把修改 ACL、关闭沙箱或完全访问模式写成通用安装步骤。
下次讨论：目标操作系统、全新安装还是迁移、代码备份方式、数据恢复点、秘密管理、验收标准。
