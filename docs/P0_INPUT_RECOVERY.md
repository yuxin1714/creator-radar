# 辅助输入恢复

2026-09-09：修改意见和作品标签/收藏/归档表单按项目/作品 ID 暂存于当前浏览器标签页。刷新或同标签页离开再返回可以恢复；不会自动提交生成、归档或覆盖服务器数据。

- 修改意见最多 5000 字符，恢复后标明尚未提交生成；归档项目不可编辑修改意见。
- 作品管理恢复保留原始版本号。其他页面已有新版本时，显示最新标签、收藏及归档值，先对照确认再允许保存。继续输入、刷新不会绕过确认。
- 成功保存清除管理表单暂存；“还原已保存设置”放弃本地填写。存储不可用时提示复制保留。
- sessionStorage 是当前标签页的辅助保护，不是跨浏览器、跨设备备份，也不承诺关闭标签页后保留。正式草稿仍需保存。

浏览器验证：修改意见刷新恢复、未提交标签恢复、其他窗口修订后的保存阻止与确认、成功后清除暂存；验证采用接口模拟，不修改真实作品或草稿。前端类型检查及生产构建通过。
