# Docker Compose local development only. Never reuse these credentials in production.
POSTGRES_DB=creator_radar
POSTGRES_USER=creator_radar
POSTGRES_PASSWORD=local_only_change_me
POSTGRES_PORT=5432
REDIS_PORT=6379
API_PORT=8000
WEB_PORT=3000
# Provider keys belong in apps/api/.env when provider integrations are implemented.
